import { ProductType } from "@/types/product.types";
type ProductCardProps = {
  product: ProductType;
}
const Hom  = ({product}:ProductCardProps) => {
  return (
    <div>Hom page</div>
  )
}
export default Hom